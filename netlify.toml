[build]
  publish = "public"

# Proxy todas las llamadas /api/* al backend en Railway
# IMPORTANTE: reemplaza la URL con la de tu app en Railway
[[redirects]]
  from   = "/api/*"
  to     = "https://TU-APP.up.railway.app/api/:splat"
  status = 200
  force  = true
  headers = { X-From = "Netlify" }
